"""Paper A figure: progressive distortion-model unlock (baseline → tilt → spherical → dipole → trefoil → octupole)."""

from __future__ import annotations

import sys


def main() -> int:
    sys.stderr.write("midas-calib-fig-distortion: implementation pending.\n")
    return 2
