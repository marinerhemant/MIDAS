"""Visualization submodule for calibration inputs/outputs.

`viz.static` is headless-safe matplotlib (requires [viz] extra).
`viz.viewer` is an interactive PyQt5 viewer (requires [viz-gui] extra).
Both are imported lazily so core-only installs stay lightweight.
"""
