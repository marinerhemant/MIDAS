"""Optional Streamlit-based GUIs.

Install the ``[gui]`` extra to enable:

    pip install 'midas-integrate-v2[gui]'

Then launch with:

    streamlit run -m midas_integrate_v2.gui.ring_picker -- params.txt image.tif
"""
